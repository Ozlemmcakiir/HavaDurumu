{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "havadurumu.serviceAccountName" . }}
  labels:
    {{- include "havadurumu.labels" . | nindent 4 }}
automountServiceAccountToken: false
{{- end }}
 