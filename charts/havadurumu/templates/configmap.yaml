apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "havadurumu.name" . }}
  labels:
    {{- include "havadurumu.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}